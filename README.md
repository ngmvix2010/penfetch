# penfetch
A simple and beautiful system fetch utility for PentaxiumOS

# To add icon feature
require: ttf-jetbrainsmono-nerd
add file config.ini in `/etc/xdg/penfetch/` or `~/.config/penfetch` with content:
```
use_icon = true
```
